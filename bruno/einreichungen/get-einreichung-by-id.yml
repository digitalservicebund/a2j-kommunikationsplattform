info:
  name: Get Einreichung By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/verfahren/{{VERFAHREN_ID}}/einreichungen/{{EINREICHUNG_ID}}"
  auth: inherit

settings:
  encodeUrl: true
