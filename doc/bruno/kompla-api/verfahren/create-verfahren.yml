info:
  name: Create Verfahren
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/verfahren"
  body:
    type: json
    data: |-
      {
        "safe_id": "{{SAFE_ID}}",
        "verfahren": {
          "verfahrensgegenstand": "Zahlungsklage",
          "kurzrubrum": null,
          "gericht_id": "{{GERICHT_ID}}",
          "beteiligungen": null
        }
      }
  auth: inherit

settings:
  encodeUrl: true
