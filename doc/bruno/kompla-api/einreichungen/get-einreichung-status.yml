info:
  name: Get Einreichung Validierungsstatus
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/verfahren/{{VERFAHREN_ID}}/einreichungen/{{EINREICHUNG_ID}}/validierungsstatus"
  auth: inherit

settings:
  encodeUrl: true
