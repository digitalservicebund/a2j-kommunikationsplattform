info:
  name: Create Einreichung
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/verfahren/{{VERFAHREN_ID}}/einreichungen"
  body:
    type: json
    data: |-
      {
        "name": "Klageeinreichung"
      }
  auth: inherit

settings:
  encodeUrl: true
